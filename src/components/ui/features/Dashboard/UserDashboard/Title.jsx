const Title = ({ title }) => {
  return (
    <h2 className="primary-text-color dark:text-white leading-inherit text-2xl md:text-[34px] font-semibold">
      {title}
    </h2>
  );
};

export default Title;
